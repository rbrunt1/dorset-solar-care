{
  "name": "solarmot-site",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing, lead-capture, booking and subscription sign-up site for SolarMOT, with a Netlify Functions backend.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
